node_modules
dist
.env
.DS_Store

# Database (personal flight data — do not commit)
server/flightlogger.db
server/*.db
server/*.db-shm
server/*.db-wal

# Compiled native binaries
*.node

# Claude Code local settings
.claude/settings.local.json
